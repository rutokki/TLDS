<UserControl x:Class="TLDSDashBoard.Views.ToastView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" IsHitTestVisible="False">

    <Border HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,24,24"
            Background="{StaticResource PanelHoverBgBrush}" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="1"
            CornerRadius="9" Padding="16,12"
            Visibility="{Binding Toast, Converter={StaticResource NullToVisibilityConverter}}">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.5" BlurRadius="28" ShadowDepth="6" Direction="270"/>
        </Border.Effect>
        <TextBlock Text="{Binding Toast}" FontSize="13"/>
    </Border>
</UserControl>
