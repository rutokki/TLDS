<UserControl x:Class="TLDSDashBoard.Views.GraphDetailSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:TLDSDashBoard.Views"
             mc:Ignorable="d">

    <UserControl.Resources>
        <!-- Left label for one lane: color dot + name (top), current/hovered value (middle), id (bottom).
             Same visual as MetricGroupView's lane label — kept as a separate copy here because this page's
             lanes additionally live under a shared zoom/pan overlay instead of only a hover crosshair. -->
        <Style x:Key="DetailLaneLabelStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush1}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="6,8"/>
            <Setter Property="Height" Value="90"/>
        </Style>
    </UserControl.Resources>

    <!-- DataContext is overridden here (not on the UserControl root) so a Visibility binding set from
         outside (against MainViewModel) keeps working — see MetricGroupView for the same pattern. -->
    <ScrollViewer DataContext="{Binding GraphDetailSearch}" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel>

            <!-- Filters (own row) + action button (own row) — see other search pages for why these are split. -->
            <Border Style="{StaticResource Card}" Padding="16,12" Margin="0,0,0,20">
                <StackPanel>
                    <WrapPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="기간" Style="{StaticResource FilterLabel}"/>
                        <views:DateRangeFilterView Filter="{Binding Range}" Margin="0,0,20,6"/>

                        <TextBlock Text="장치" Style="{StaticResource FilterLabel}"/>
                        <ComboBox ItemsSource="{Binding AvailableTracks}" SelectedItem="{Binding SelectedTrack}"
                                  ItemTemplate="{StaticResource NameItemTemplate}" Margin="0,0,0,6"/>
                    </WrapPanel>

                    <WrapPanel Orientation="Horizontal">
                        <Button Style="{StaticResource FilterActionButton}" Command="{Binding SearchCommand}" Margin="0,0,8,0">
                            <TextBlock Text="조회"/>
                        </Button>
                        <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource HintText}" VerticalAlignment="Center"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Padding="20">
                <StackPanel>
                    <DockPanel LastChildFill="False" Margin="0,0,0,10">
                        <TextBlock DockPanel.Dock="Left" Text="TX/RX 채널 상세 (12개 채널, 기본 1시간)" Style="{StaticResource SectionTitle}"/>
                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                            <Rectangle Width="14" Height="2" Fill="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="측정" FontSize="10.5" Foreground="{StaticResource TextDimBrush}" Margin="4,0,14,0"/>
                            <Rectangle Width="14" Height="2" Fill="{StaticResource CheckedLineBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="점검" FontSize="10.5" Foreground="{StaticResource TextDimBrush}" Margin="4,0,14,0"/>
                            <Rectangle Width="14" Height="2" Fill="{StaticResource MinimumLineBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="최소" FontSize="10.5" Foreground="{StaticResource TextDimBrush}" Margin="4,0,14,0"/>
                            <TextBlock Text="휠: 확대·축소 / 드래그: 이동" FontSize="11"
                                       Foreground="{StaticResource TextDimBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DockPanel>

                    <!-- One stacked lane per channel — each with its OWN real value axis (not normalized/overlaid,
                         since units differ: V/A/Hz/ms). A single ChartArea overlay spans the whole stack and
                         drives shared zoom/pan/hover across every lane at once (see code-behind). -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left: name / current-or-hovered value / id, one row per channel -->
                        <ItemsControl Grid.Column="0" ItemsSource="{Binding Series}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource DetailLaneLabelStyle}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <StackPanel Grid.Row="0" Orientation="Horizontal">
                                                <Ellipse Width="8" Height="8" VerticalAlignment="Center" Margin="0,0,6,0"
                                                         Fill="{Binding ColorHex, Converter={StaticResource HexToBrushConverter}}"/>
                                                <TextBlock Text="{Binding Name}" FontSize="11.5" Foreground="#FFBFC5CE"/>
                                            </StackPanel>

                                            <TextBlock Grid.Row="1" VerticalAlignment="Center" FontFamily="{StaticResource MonoFont}"
                                                       FontSize="16" FontWeight="SemiBold">
                                                <Run Text="{Binding Value, Mode=OneWay}"/>
                                                <Run Text="{Binding Unit, Mode=OneWay}" FontSize="10.5" FontWeight="Normal" Foreground="{StaticResource TextDimBrush}"/>
                                            </TextBlock>

                                            <TextBlock Grid.Row="2" Text="{Binding Id}" FontSize="9.5" FontFamily="{StaticResource MonoFont}"
                                                       Foreground="{StaticResource TextDimBrush}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Right: one chart per channel, stacked, each with its own top(max)/bottom(min) axis labels -->
                        <Grid Grid.Column="1">
                            <ItemsControl ItemsSource="{Binding Series}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Height="90" BorderBrush="{StaticResource BorderBrush1}" BorderThickness="0,0,0,1">
                                            <Grid>
                                                <Viewbox Stretch="Fill">
                                                    <Canvas Width="900" Height="90">
                                                        <Path Data="{Binding CheckedLinePath, Converter={StaticResource PathDataConverter}}"
                                                              Stroke="{StaticResource CheckedLineBrush}" StrokeThickness="1.75" StrokeDashArray="4,3"/>
                                                        <Path Data="{Binding MinimumLinePath, Converter={StaticResource PathDataConverter}}"
                                                              Stroke="{StaticResource MinimumLineBrush}" StrokeThickness="1.75" StrokeDashArray="4,3"/>
                                                        <Path Data="{Binding Path, Converter={StaticResource PathDataConverter}}"
                                                              Stroke="{Binding ColorHex, Converter={StaticResource HexToBrushConverter}}"
                                                              StrokeThickness="2"/>
                                                    </Canvas>
                                                </Viewbox>
                                                <TextBlock Text="{Binding AxisMaxLabel}" FontSize="9" FontFamily="{StaticResource MonoFont}"
                                                           Foreground="{StaticResource TextDimBrush}" HorizontalAlignment="Right"
                                                           VerticalAlignment="Top" Margin="0,2,4,0"/>
                                                <TextBlock Text="{Binding AxisMinLabel}" FontSize="9" FontFamily="{StaticResource MonoFont}"
                                                           Foreground="{StaticResource TextDimBrush}" HorizontalAlignment="Right"
                                                           VerticalAlignment="Bottom" Margin="0,0,4,2"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!-- Shared overlay: transparent so it captures the mouse across the whole stack,
                                 stretches to the same size as the ItemsControl above it (same Grid cell).
                                 Unlike MetricGroupView's hover-only overlay, this one also drives zoom/pan. -->
                            <Grid x:Name="ChartArea" Background="Transparent" Cursor="SizeWE"
                                  MouseMove="ChartArea_MouseMove" MouseLeave="ChartArea_MouseLeave"
                                  MouseWheel="ChartArea_MouseWheel"
                                  MouseLeftButtonDown="ChartArea_MouseLeftButtonDown"
                                  MouseLeftButtonUp="ChartArea_MouseLeftButtonUp">
                                <Line x:Name="CrosshairLine" Y1="0" Stroke="White"
                                      StrokeThickness="1" StrokeDashArray="3,2" Visibility="Collapsed" IsHitTestVisible="False"/>

                                <!-- Mouse-following tooltip, implemented as a Popup (its own top-level window layer)
                                     instead of a plain positioned Border: a Border repositioned via Margin sits in
                                     the SAME visual tree ChartArea is hit-testing against, so moving it under a
                                     stationary cursor could itself trigger WPF's synthetic mouse-move re-evaluation
                                     and fight the next real MouseMove, producing exactly the jumpy/stuck behavior
                                     seen when that was tried. A Popup renders outside that tree, so repositioning
                                     it via HorizontalOffset/VerticalOffset in code-behind can't feed back into
                                     hit-testing. -->
                                <Popup x:Name="HoverTooltip" Placement="Relative" AllowsTransparency="True"
                                       IsHitTestVisible="False" Focusable="False" StaysOpen="True">
                                    <Border Background="{StaticResource PanelHoverBgBrush}" BorderBrush="{StaticResource BorderLightBrush}"
                                            BorderThickness="1" CornerRadius="6" Padding="10,8">
                                        <StackPanel>
                                            <TextBlock Text="{Binding HoverTimeLabel}" FontSize="10.5" FontFamily="{StaticResource MonoFont}"
                                                       Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                            <ItemsControl ItemsSource="{Binding Series}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal" Margin="0,1">
                                                            <Ellipse Width="7" Height="7" VerticalAlignment="Center" Margin="0,0,6,0"
                                                                     Fill="{Binding ColorHex, Converter={StaticResource HexToBrushConverter}}"/>
                                                            <TextBlock FontSize="10.5" FontFamily="{StaticResource MonoFont}">
                                                                <Run Text="{Binding Name, Mode=OneWay}" Foreground="{StaticResource TextSecondaryBrush}"/>
                                                                <Run Text=" : "/>
                                                                <Run Text="{Binding Value, Mode=OneWay}" FontWeight="SemiBold"/>
                                                                <Run Text="{Binding Unit, Mode=OneWay}" Foreground="{StaticResource TextDimBrush}"/>
                                                            </TextBlock>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </Popup>

                                <Border Background="{StaticResource PanelHoverBgBrush}" BorderBrush="{StaticResource BorderLightBrush}"
                                        BorderThickness="1" CornerRadius="5" Padding="6,3" Margin="6"
                                        HorizontalAlignment="Right" VerticalAlignment="Top" IsHitTestVisible="False">
                                    <TextBlock Text="{Binding WindowLabel}" FontSize="10.5" FontFamily="{StaticResource MonoFont}"
                                               Foreground="{StaticResource TextSecondaryBrush}"/>
                                </Border>
                            </Grid>
                        </Grid>
                    </Grid>

                    <!-- Shared time axis, indented to align under the chart column -->
                    <Grid Margin="120,4,0,0">
                        <TextBlock Text="{Binding TimeStartLabel}" FontSize="9.5" FontFamily="{StaticResource MonoFont}"
                                   Foreground="{StaticResource TextDimBrush}" HorizontalAlignment="Left"/>
                        <TextBlock Text="{Binding TimeMidLabel}" FontSize="9.5" FontFamily="{StaticResource MonoFont}"
                                   Foreground="{StaticResource TextDimBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding TimeEndLabel}" FontSize="9.5" FontFamily="{StaticResource MonoFont}"
                                   Foreground="{StaticResource TextDimBrush}" HorizontalAlignment="Right"/>
                    </Grid>

                    <DockPanel LastChildFill="False" Margin="120,10,0,0">
                        <TextBlock DockPanel.Dock="Left" Text="{Binding RangeLabel}" FontSize="11" Foreground="{StaticResource TextDimBrush}"/>
                        <Button DockPanel.Dock="Right" Style="{StaticResource FilterActionButton}" Command="{Binding ResetZoomCommand}">
                            <TextBlock Text="줌 초기화"/>
                        </Button>
                    </DockPanel>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
